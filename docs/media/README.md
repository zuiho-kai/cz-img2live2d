# 小征演示来源

首页 `changzheng-demo.gif` 节选自 `changzheng-7d443a0a33` 的实际浏览器排练录像，原视频约 31.84 秒、640×800，GIF 取第 10–30 秒，按原速度播放，缩至 400×500、10 fps。

角色使用 [默认模型](../../examples/default-live2d/README.md)，模型和运行脚本与长征当前模拟器副本的哈希一致。未重新生成角色画面，也没有用剪辑放大动作。

口型由已有录音的 PCM 数据驱动；录屏文件没有声音。完整 MP4 保留原录像时长，可在 [Release 附件](https://github.com/zuiho-kai/cz-img2live2d/releases/download/v0.1.0/changzheng-demo.mp4) 下载。模型来源和文件哈希见默认模型目录的 `provenance.json`。
